// Counter.js
import React from 'react';

const Counter = () => {
  return (
    <div>
      <p>{0}</p>
      <button onClick={() => {}}>+</button>
      <br />
      <button onClick={() => {}}>Reset</button>
    </div>
  );
};

export default Counter;
